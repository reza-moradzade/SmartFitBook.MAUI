@page "/"
@using SmartFitBook.MAUI.Components.Layout
@inject NavigationManager NavigationManager
@layout EmptyLayout

@code {
    protected override void OnInitialized()
    {
        // Directly redirect to the Login page
        NavigationManager.NavigateTo("/login", true);
    }
}
